{
  "name": "@shelf/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./client": {
      "types": "./src/client.ts",
      "default": "./src/client.ts"
    }
  },
  "scripts": {
    "build": "prisma generate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:prepare-migration": "prisma generate && prisma migrate dev --create-only --skip-seed && tsx src/post-migration.ts",
    "db:deploy-migration": "prisma migrate deploy && prisma generate",
    "db:prepare-migration:staging": "dotenv -e ../../.env.staging -- prisma generate && prisma migrate dev --create-only --skip-seed && tsx src/post-migration.ts",
    "db:deploy-migration:staging": "dotenv -e ../../.env.staging -- prisma migrate deploy && prisma generate",
    "db:deploy-migration:production": "dotenv -e ../../.env.production -- prisma migrate deploy && prisma generate",
    "db:reset": "prisma migrate reset",
    "db:reset:staging": "dotenv -e ../../.env.staging -- prisma migrate reset",
    "db:deploy-migration:demo": "dotenv -e ../../.env.demo -- prisma migrate deploy --config prisma.config.postgres.ts && prisma generate --config prisma.config.postgres.ts",
    "db:deploy-migration:karofi": "dotenv -e ../../.env.karofi -- prisma migrate deploy --config prisma.config.postgres.ts && prisma generate --config prisma.config.postgres.ts",
    "db:deploy-migration:cpanel": "dotenv -e ../../.env.cpanel -- prisma migrate deploy && prisma generate",
    "db:migrate:mysql-baseline": "dotenv -e ../../.env.cpanel -- prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations-mysql/0001_baseline/migration.sql"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "@shelf/typescript-config": "workspace:*",
    "@types/node": "^22.19.17",
    "dotenv": "^17.4.1",
    "dotenv-cli": "^11.0.0",
    "prisma": "^6.19.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
